<h3>以下の内容で、投稿してもよいですか？</h3>
<%= form_with(model: @picture, local: true) do |form| %>
  <%= form.hidden_field :title %>
  <%= form.hidden_field :content %>
  <p>Title:<%= @picture.title %></p>
  <p>Content:<%= @picture.content %></p>
  <%= form.submit  "投稿する" %>
<% end %>